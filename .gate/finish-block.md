# Finish block

TICKET: https://github.com/Prinzaessin86/tiara-site/issues/72
TICKET: https://github.com/Prinzaessin86/tiara-site/issues/73
TICKET: https://github.com/Prinzaessin86/tiara-site/issues/77

## CHANGED

`tiara-site`
- `index.html` — `board-v89`, commit `45554d3`. The #72 rework.
- `.gate/finish-block.md` — this file.

`tiara` (private)
- `docs/HOW-TIARA-WORKS.md` — the workflows section moved from Runbook to §4a. Stamped `board-v89`.
- `compliance.json` — regenerated by the new scanner, so it carries the App Store Connect app ids.
  Both in commit `3901f2d`.

`_bootstrap`
- `scripts/compliance.mjs` — records `id`, `enabled` and `url` per cloud workflow. Commit `e47cf3d`.

Nothing was written to `scripts/hooks/pre-commit`.

## What changed

**#72, reworked to Princess's comment** ("move this information out of the runbook to the project
detail site and show the chips as clickable link to the respective workflow, github actions isn't
needed"):

- The section is off the Runbook and on the project page, headed *Xcode Cloud · what actually runs*,
  between Open work and Last 5 commits. `#runbookworkflows` and `renderRunbookWorkflows` deleted.
- Chips link to `https://appstoreconnect.apple.com/apps/<ascAppId>/ci`. The scanner learned the app
  id from `/v1/ciProducts/<id>/app` to make that possible.
- GitHub Actions dropped entirely.
- Chip contrast measured on each chip's own fill: 5.32:1 blue, 5.71:1 grey, 4.94:1 amber. The
  obvious blue for that fill was 3.45:1.

**#73** — follow-up filed as
[_bootstrap#87](https://github.com/Prinzaessin86/_bootstrap/issues/87), the conform check that would
have caught the stray file at commit time. Linked from #73, and #87's body links back.

**#77** — the wiring script posted on the ticket for Princess to run, having been run end to end in
a throwaway clone first.

## VERIFIED

- `node --check` on both `<script>` blocks; `python3 scripts/lint-tokens.py index.html` green.
- **#72 against a deliberately mutated scan**, in headless Chrome, because the live one has none of
  the interesting cases. Rainybow renders a linked chip whose href is
  `https://appstoreconnect.apple.com/apps/6795511046/ci`; Sparkle Reef says "no Xcode Cloud workflow
  · checked"; Bubbles with `xcodeCloud:null` says "unknown · no App Store Connect key at scan time";
  Octobrain with the whole `workflows` key deleted says "not scanned for workflows" and does not
  throw; Dressie with `missing:true` says nothing could be checked; Poda with `url:null` renders an
  unlinked chip. No `.wfact` element exists anywhere. No `#runbookworkflows` element exists.
- The "a chip opens this app's Xcode Cloud page" caption appears only when a link is on screen.
- Project page screenshotted at 1400px.
- **#77's script proved in a throwaway git repo** with today's `index.html`, the linter and the hook,
  `core.hooksPath` set: patch applied, `bash -n` parsed it, a clean version-bumped change committed,
  a canary carrying `#ff00aa` and `13px` was refused `rc=1` naming both at `index.html:396`, the same
  canary with `TIARA_SKIP_DOC=1` was refused too, and `git log` confirmed it never landed.

## NOT VERIFIED

- **Nobody clicked a workflow chip while signed in to App Store Connect.** Signed out, ASC 302s every
  path under `/apps/<id>/` to its login page carrying the original `targetUrl`, so curl cannot tell a
  good path from a bad one; only `/ci` with no app in front is a real 404. The URL is Apple's
  documented location for the Xcode Cloud page, not something a check confirmed. One click settles
  it. This is the one claim on the page I cannot stand behind on my own.
- The link is **per app, not per workflow**, which is not literally what the comment asked for. The
  API exposes no per-workflow web address, so the alternative was a fabricated path. The caption on
  the page says which it is.
- The real gate is still unwired: the script is on #77, unrun by me, because an agent may not edit a
  gate script.
- Still nothing against a real GitHub board, no hand-clicked browser, no phone.
- Nothing pushed. Six local commits now across three repos.

## Notes

`compliance.json` is regenerated on a schedule that also pushes it. That schedule will now pick up
the new scanner on its own, so the ids will refresh without anybody doing anything.
